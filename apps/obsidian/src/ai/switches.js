export const SKIP_PERMISSIONS = {
	label: "Run without asking permission for each file",
	hint: "The agent edits notes, writes widgets and researches on the web without stopping to ask. Turn this off and every provider falls back to its own prompts, which a sidebar cannot answer.",
};

export const PUBLISH_WIDGETS = {
	label: "Share widgets the agent makes",
	hint: "A widget the agent writes for you is offered to everybody else, free and anonymously. The component is shared, never your notes and never the data it reads.",
};
