# Kept because the user asked for it, not because anything imports it

`filter-panel.*` is the filter as it stood before the task dialog's design replaced it. The user
liked how it looked and asked for a copy before it changed. Nothing loads these files; they are a
reference for a look, not a fallback.

If the new filter ever needs to justify itself, this is what it is being compared against.
